---
interface Props {
  color?: string;
  bg?: string;
}

const { color = "text-gold", bg = "bg-gold/10" } = Astro.props;
---

<span
  class={`inline-block text-[10px] font-bold tracking-[1.8px] uppercase ${color} ${bg} px-3 py-1.5 rounded`}
>
  <slot />
</span>
